name: Update Docs

on:
 push:
  branches:
    - master

jobs:
  update-docs:
    runs-on: macos-11
    
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Update Documentation
        run: ./scripts/update-gh-pages